use crate::board::Board;

enum Move {
    Right,
}

pub fn generate_solve(board: Board) -> Vec<String> {
    let full_solve: Vec<String> = vec![];
    Vec::new()
}

#[cfg(test)]
mod tests {

    #[test]
    fn test() {}
}
